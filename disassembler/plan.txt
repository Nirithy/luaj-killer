1. Implement a C++ tool in a new folder (e.g., `luaj-disassembler`) that reads `.luac` files generated by Luaj.
2. Ensure the parser logic closely models `org.luaj.vm2.compiler.LoadState` and `DumpState`.
3. Read the file header to detect format, endianness, int sizes, and number formats.
4. Read the Prototypes recursively, including instructions, constants, upvalues, and debug info.
5. Create a pretty-printing function to display instructions and their opcodes. Use `Lua.java` to map opcodes to mnemonics and decode `A, B, C, Bx, sBx, Ax` correctly.
6. Provide a clean `Makefile` to compile it.
7. Include a pre-commit step to make sure the code compiles and can execute.
